import math
t1 = float(input('1 '))
g1 = float(input('2 '))
t2 = float(input('3 '))
g2 = float(input('4 '))

distance = 6371.1 * math.acos(math.sin(t1) * math.sin(t2) + math.cos(t1) * math.cos(t2) * math.cos(g1 - g2))

print(distance)
